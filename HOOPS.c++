#include <bits/stdc++.h> // HEADER FILE

#include <iostream>
using namespace std;

int main() // MAIN DEFINATION
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int n;
        cin >> n;
        cout << (n + 1) / 2 << endl;
    }
    return 0;
}