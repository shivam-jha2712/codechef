#include <bits/stdc++.h> // HEADER FILE

#include <iostream>
using namespace std;

int main() // MAIN DEFINATION
{
    int t;
    cin >> t;
    int arr[t];
    for (int i = 1; i < t + 1; i++)
    {
        cin >> arr[i];
    }

    return 0;
}